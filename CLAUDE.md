# 프로젝트 규칙

- 파일 경로를 코드에 하드코딩하지 않는다. 사용자가 그때그때 업로드/지정하는 파일을 사용한다.
- 엑셀(.xlsx, .xls) 및 CSV 파일은 앱 폴더 바깥에 둔다. 앱 폴더 안에 있다면 `.gitignore`에 등록해 버전관리에서 제외한다.
- API 키 등 민감한 값은 코드에 직접 넣지 않고 `.env` 파일에서 읽어온다. `.env`는 `.gitignore`에 포함한다.
- 답변은 항상 한국어로 한다.
